{
  "name": "{{ name }}",
  "description": "{{ description }}",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "serve": "sl-theme serve",
    "zip": "sl-theme zip"
  },
  "devDependencies": {
    "@yy/sl-theme-cli": "latest"
  }
}
